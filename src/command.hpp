#pragma once

#include <string>
#include <vector>

void command_new(std::vector<std::string> args);
void command_test(std::vector<std::string> args);
void command_run(std::vector<std::string> args);
void command_build(std::vector<std::string> args);
void command_include(std::vector<std::string> args);
void command_tidy(std::vector<std::string> args);
